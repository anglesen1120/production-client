/*
 * WorkerPage Messages
 *
 * This contains all the text for the WorkerPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WorkerPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorkerPage container!',
  },
});
