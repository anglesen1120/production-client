/*
 * Statistics Messages
 *
 * This contains all the text for the Statistics component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Statistics';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Statistics component!',
  },
});
